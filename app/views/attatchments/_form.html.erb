<div class="row">
  <div class="text-center">
    <div class="col-md-6 col-md-offset-3" style="padding: 10px 0px 0px 0px;">
      <h1 style="font-size:200%;">Nueva publicación</h1>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-sm-2">
    <%=render 'static/menu_publicaciones'%>
  </div>

  <div class="col-sm-10">
    <div class="main-content">
      <div class="form-basic">

		<%= form_for(attatchment) do |f| %>
		  <% if attatchment.errors.any? %>
			<div id="error_explanation">
			  <h2><%= pluralize(attatchment.errors.count, "error") %> prohibited this attatchment from being saved:</h2>

			  <ul>
			  <% attatchment.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			  <% end %>
			  </ul>
			</div>
		  <% end %>

		  <div class="form-row">
			<%= f.label :'Cargar solicitud' %>
			<%= f.file_field :url%>
			<%= f.hidden_field :category, :value => '1'%>
		  </div>

		  
		  <div class="form-row">
			<%= f.label :'Cargar manuscrito' %>
			<%= f.file_field :url %>
			<%= f.hidden_field :category, :value => '1'%>
		  </div>

		  <div class="actions">
			<%= f.submit :"Subir archivos"%>
		  </div>
		<% end %>
		</div>
	  </div>
	</div>
</div>