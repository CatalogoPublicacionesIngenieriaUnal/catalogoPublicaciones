<%= form_for(professor_application_request) do |f| %>
  <% if professor_application_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professor_application_request.errors.count, "error") %> prohibited this professor_application_request from being saved:</h2>

      <ul>
      <% professor_application_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
