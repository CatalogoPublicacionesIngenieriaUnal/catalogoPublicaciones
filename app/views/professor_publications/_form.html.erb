<%= form_for(professor_publication) do |f| %>
  <% if professor_publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professor_publication.errors.count, "error") %> prohibited this professor_publication from being saved:</h2>

      <ul>
      <% professor_publication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
